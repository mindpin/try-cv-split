%h1 Listing Comments
%table.comments.ui.celled.table
  %thead
    %tr
      %th Title
      %th Body
      %th Published
      %th{:colspan => "3"}
  %tbody
    - @comments.each do |comment|
      %tr
        %td= comment.title
        %td= comment.body
        %td= comment.published
        %td= link_to 'Show', comment
        %td= link_to 'Edit', edit_comment_path(comment)
        %td
          %a.destory{href: 'javascript:;', data: {id: comment.id}} Destory

%a.ui.labeled.icon.button.green{:href => new_comment_path}
  %i.icon.add
  New Comment
