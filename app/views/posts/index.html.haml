%p#notice= notice
%h1 Listing Posts
%table.posts.ui.celled.table
  %thead
    %tr
      %th Title
      %th Body
      %th Published
      %th{:colspan => "3"}
  %tbody
    - @posts.each do |post|
      %tr
        %td= post.title
        %td= post.body
        %td= post.published
        %td= link_to 'Show', post
        %td= link_to 'Edit', edit_post_path(post)
        %td
          %a.destory{href: 'javascript:;', data: {id: post.id}} Destory

%a.ui.labeled.icon.button.green{:href => new_post_path}
  %i.icon.add
  New Post
