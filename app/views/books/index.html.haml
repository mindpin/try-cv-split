%h1 Listing Books
%table.books.ui.celled.table
  %thead
    %tr
      %th Title
      %th Body
      %th Published
      %th{:colspan => "3"}
  %tbody
    - @books.each do |book|
      %tr
        %td= book.title
        %td= book.body
        %td= book.published
        %td= link_to 'Show', book
        %td= link_to 'Edit', edit_book_path(book)
        %td
          %a.destory{href: 'javascript:;', data: {id: book.id}} Destory

%a.ui.labeled.icon.button.green{:href => new_book_path}
  %i.icon.add
  New Book
